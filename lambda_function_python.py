# syntax of lambda function:

# lambda argument = expression
#eg: lambda x : x+1


# map function:  map(function,iterable) :returns an iterable object. the function is applied to each value of iterable

# filter function:  returns an iterable object.   filter( filtering function, sequence . it will filter the sequence based on filtering function)

